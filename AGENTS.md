# Agent Instructions

- Only add, commit or push files on git when explicitly told so.
- If the user's query starts with `Q:` or `Qustion:`, answer the question only and do not implement changes or perform any other actions.
- Bash is preferred over powershell, but both are allowed.
- You can find bash here c:\Program Files\Git\bin\bash.exe
- Give all powershell/bash commands etc 5 times a long timeout as normal.
- This project contains many separate minor project. Only work in the sub-directory indicated (one at a time). Exception for when updating GamesAndStuff_JS.html.
- Python is usually here: c:\Portable\WinPython\python\python.exe
- NodeJs is usually here: c:\Portable\NodeJS\node.exe
- Playwright is usually here: c:\Portable\Playwright
- Keep your replies as short as possible
- If asked for changes that are problematic or leads to fragile solution in current design: Stop, notify the user and ask for confirmation before implementing.
